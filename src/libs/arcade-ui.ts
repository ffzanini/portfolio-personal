/** Shared sharp-corner tokens for Arcade / pixel UI */
export const arcadeSquare = "rounded-none";

/**
 * Flip to `true` when Arcade is ready to publish.
 * While `false`, `/arcade` and `/arcade/pixel-art` show the under-construction page.
 */
export const ARCADE_ENABLED = false;
